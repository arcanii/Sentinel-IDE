# Build output (CMake/Ninja); regenerated by scripts\build.bat
/build/

# Compiler/linker artifacts anywhere (snc/MSVC) — e.g. examples\target\
target/
*.o
*.obj
*.exe
*.lib
*.a
*.pdb
*.exp
*.ilk

# snc also emits an EXTENSIONLESS PE beside the source (examples\crypto is an
# MZ binary, not a folder), which the *.exe rule above does not catch.
/examples/crypto
/examples/hello

# Sealed projects, signing keys, and unsealed working copies — never commit
*.sealed
*.key
*-unsealed/

# Agent/editor tooling and local config (not part of the project)
/.claude/
/_bmad/

# OS / editor cruft
Thumbs.db
desktop.ini
*.user
