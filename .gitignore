# Build output (CMake/Ninja); regenerated by scripts\build.bat
/build/

# Compiler/linker artifacts anywhere (snc/MSVC) — e.g. examples\target\
target/
*.o
*.obj
*.exe
*.lib
*.a
*.pdb
*.exp
*.ilk

# snc also emits an EXTENSIONLESS PE beside the source (examples\crypto is an
# MZ binary, not a folder), which the *.exe rule above does not catch.
/examples/crypto
/examples/hello

# Vendored third-party binaries are deliberately committed — the *.lib and *.dll
# rules above are about OUR build output, not dependencies. Without these negations
# WinSparkle.lib is silently dropped and the updater build breaks on a fresh clone.
!/third_party/**/*.lib
!/third_party/**/*.dll

# Sealed projects, signing keys, and unsealed working copies — never commit
*.sealed
*.key
*-unsealed/

# Agent/editor tooling and local config (not part of the project)
/.claude/
/_bmad/

# OS / editor cruft
Thumbs.db
desktop.ini
*.user
