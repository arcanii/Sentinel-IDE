// SPDX-License-Identifier: GPL-3.0-or-later
#pragma once
#include <windows.h>

namespace sentinelide {

// Show the modal, dark-themed About dialog (S2 shield icon + name/version/tagline).
void showAboutDialog(HWND owner);

}  // namespace sentinelide
